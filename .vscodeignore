# Extension is fully bundled (dist/extension.js + media/*); ship only runtime assets.
# README screenshots resolve to the GitHub repository (repository field in
# package.json), so the local artifacts/ copies stay out of the VSIX.
artifacts
PROGRESS.md
.gitignore
.temp
.vscode-test
test-results
playwright-report
playwright.config.ts
*.log
src
tests
docs
node_modules
.vscode
**/*.map
*.vsix
tsconfig.json
tsconfig.webview.json
vite.config.ts
esbuild.config.mjs
